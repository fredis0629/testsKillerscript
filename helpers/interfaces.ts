export interface TagIdentety {
  attributeName: string;
  attributeValue: string;
}
export interface Elements {
  name: string;
  tagName: string;
  pid: string;
  startIdNumber: number;
  count: number;
}
